#include "std_lib_facilities.h"
#include <utility>
int main() {
	int a, b, c;
	cout << "Please input 3 integer values\n";
	cin >> a >> b >> c;
	if (a <= b && a <= c) {
	
	}	
	if (b >= a && b <= c) {


	}	
}
